






contract C {
  

  

  uint256 tierElements;
uint256 j;
uint256[] tiersData;
uint256[] tiers;

  function foo() public {
    
for(uint256 i = 0; i <= tierElements; i += tierElements){
tiersData[j++] = ((uint256(1e23)) / (tiers[i]));
tiersData[j++] = 0;
tiersData[j++] = uint256(tiers[((i) + (1))]);
tiersData[j++] = uint256(tiers[((i) + (2))]);
tiersData[j++] = 0;
tiersData[j++] = 0;
tiersData[j++] = uint256(tiers[((i) + (4))]);
tiersData[j++] = 0;
tiersData[j++] = 0;
tiersData[j++] = 0;
tiersData[j++] = uint256(tiers[((i) + (5))]);
tiersData[j++] = uint256(tiers[((i) + (6))]);
tiersData[j++] = 1;
}


  }

  

}

//#LOOPVARS: i


