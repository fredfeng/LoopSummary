
pragma solidity ^0.5.10;



contract C {
  

  uint256 decimals;
uint256 _keys;
uint256 price;
uint256 _price;
uint256 costEth;
uint256 c_key;

  function foo() public {
    
while(_keys >= decimals){
c_key = (c_key) + (1);
_price = price(c_key);
costEth = (costEth) + (_price);
_keys = (_keys) - (decimals);
}

  }
}

//#LOOPVARS: 
