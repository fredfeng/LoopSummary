
pragma solidity ^0.5.10;



contract C {
  

  Round round;
mapping(address => uint32) uidOf;
uint256 amount;
uint i;

  function foo() public {
    
for(uint i = 0; i < (amount) / (round.min_amount); i++){
round.peoples.push(uidOf[msg.sender]);
}


  }
}

//#LOOPVARS: i

contract Round { }
